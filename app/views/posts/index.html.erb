<h1> hi </h1>
<%= @posts.each do |post| %>
  <div class="post-wrapper">
    <h2 class="title"><%= link_to post.title, post %>
      <p class="date"><%= post.created_at.strftime("%B, %d, %Y") %> </p>
  </div>
<% end %>